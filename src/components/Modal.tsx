import styles from './Modal.module.css'

interface IProps {}
const Modal = (props: IProps) => {
  return (
    <div id="modal">
      <div>
      </div>
      <div>
        <h2>Texto modal</h2>
      </div>
    </div>
  );
};

export default Modal;
